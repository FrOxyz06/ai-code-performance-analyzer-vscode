{
  "name": "performance-analyzer-vscode",
  "version": "0.2.0",
  "displayName": "Performance Analyzer",
  "description": "Compare Python function behavior and median runtime using the same benchmark format as Collab Review.",
  "engines": {
    "vscode": "^1.100.0"
  },
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "performanceAnalyzer.analyzeFile",
        "title": "Performance Analyzer: Compare File"
      }
    ],
    "configuration": {
      "title": "Performance Analyzer",
      "properties": {
        "performanceAnalyzer.pythonPath": {
          "type": "string",
          "default": "python",
          "scope": "machine",
          "description": "Python 3.10+ executable path (no command-line arguments)."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc",
    "test": "npm run compile && node --test *.test.cjs",
    "test:python": "python -m unittest -v test_benchmark.py"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^22.20.3",
    "@types/vscode": "^1.138.0",
    "typescript": "^7.0.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/FrOxyz06/ai-code-performance-analyzer-vscode.git"
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "Code execution and applying proposals require workspace trust."
    },
    "virtualWorkspaces": false
  }
}
